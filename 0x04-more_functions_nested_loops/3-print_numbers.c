#include "main.h"


/**
 * print_numbers - prints numbers between 0 to 9.
 * Return: no return.
 */
void print_numbers(void)
{
	int a;

	if (a <= '9' && a >= '0')
	{
		_putchar(a);
	}
	_putchar('\n');
}
